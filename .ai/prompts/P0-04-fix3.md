GATE 3, third pass on P0-04. Your design fixes all landed correctly and were verified in a real
browser: the fake loading skeleton is gone, the banner is named "Workspace and identity context",
there are zero pulsing elements, an h1 renders per route, nav text now computes to #6D6964 which
passes contrast, and the font stack is honest. One regression to fix.

**Never invoke a tool that runs a shell command.** Read and write files by exact path only.

DEFECT: the section description renders twice

On every route, the same sentence appears in two places: once as the `description` passed to
`PageHeader`, and again as the body of the placeholder section below the divider. Verified in the
DOM: `main` contains two `<p>` elements with identical text, for example
"Browse catalogs, schemas, and tables to see who can access them. Asset search is not available
yet." twice.

Fix by giving each of the two slots a distinct job:

- `PageHeader` keeps the `description`. That is the one-line answer to "what is this section for".
- The section body below it must NOT repeat it. Give it its own sentence that says what the person
  can do right now and what is missing, rather than restating the purpose.

Add a second group to `strings.ts`, for example `strings.unavailable`, holding one short sentence
per section for the body slot, and leave `strings.descriptions` for the header. The two must never
be the same string.

Suggested split for Data Assets, and write the same shape for asset, accessManagement, policies,
activity, platform, and notFound:
- header description: "Browse catalogs, schemas, and tables to see who can access them."
- body: "Asset search and the access table are not available yet. Use the navigation to explore the
  other sections."

Keep every other change you made. Do not reintroduce a card, centring, or `min-h-[50vh]`.

Verify by reading the files back: no string appears in both `strings.descriptions` and
`strings.unavailable`, and `routes.tsx` passes the header description and the body text from
different keys.

Report the files changed, then append one line to tasks/STATUS.md.
