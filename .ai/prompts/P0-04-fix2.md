GATE 3, second pass on task P0-04. Your previous two fixes worked: `tsconfig.json` no longer has a
`references` array and `eslint.config.js` now parses as flat config. Two new defects surfaced once
the tools could actually run. Fix exactly these.

**Do not invoke any tool that runs a shell command.** No `ls`, `npx`, `tsc`, `eslint`, or
file-existence check. Every attempt is auto-denied and ends your turn with nothing written. Read
and write files only.

DEFECT 1, TypeScript cannot type a CSS side-effect import

```
src/main.tsx(7,8): error TS2882: Cannot find module or type declarations for side-effect import
                   of './index.css'.
```

Cause: nothing declares Vite's client types, which is what provides the module declaration for
`.css` imports. Fix by creating **`frontend/src/vite-env.d.ts`** containing exactly:

```ts
/// <reference types="vite/client" />
```

`vite` 8.3.0 is installed, so those types resolve. Do not change the import in `main.tsx`, do not
rename `index.css`, and do not add a dependency.

If, after adding that file, you believe the `include` in `frontend/tsconfig.json` would not pick up
`src/vite-env.d.ts`, say so in your report rather than editing `tsconfig.json`; its `include` is
`["src", "e2e"]`, so the file is covered.

DEFECT 2, ESLint, redundant ARIA roles

```
src/app/AppShell.tsx
  106:11  error  The element ul has an implicit role of list. Defining this explicitly is
                 redundant and should be avoided   jsx-a11y/no-redundant-roles
  118:11  error  (same, second occurrence)
```

You wrote `<ul role="list">` in two places. A `ul` already has the list role, so the attribute is
redundant. **Remove `role="list"` from both elements.** Keep the elements, their classes, their
children, and every other attribute exactly as they are.

Do not silence the rule, do not add an eslint-disable comment, and do not change
`eslint.config.js`. The rule is correct.

VERIFY by reading the two files back. Confirm `frontend/src/vite-env.d.ts` exists with the
reference directive, and that `role="list"` appears nowhere in `AppShell.tsx`.

SCOPE. Create `frontend/src/vite-env.d.ts` and edit `frontend/src/app/AppShell.tsx`. Touch nothing
else, in particular not `tsconfig.json`, `eslint.config.js`, `package.json`, or any other file
under `src/`.

REPORT the exact changes and anything you were unsure about. Then append one line to
`tasks/STATUS.md` and update your row in `tasks/TASK-BOARD.md`.
